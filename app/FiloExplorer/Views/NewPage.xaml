<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FiloExplorer.Views.NewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FiloExplorer.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar HorizontalAlignment="Left" DefaultLabelPosition="Right" >
            <AppBarButton Icon="Save" Label="Save" ToolTipService.ToolTip="Save Filo Container" Click="Create_Click"/>
            <AppBarSeparator />
            <AppBarButton Icon="Document" Label="Add File" ToolTipService.ToolTip="Add Files" Click="AddFile_Click"/>
            <AppBarButton Icon="Folder" Label="Add Folder" ToolTipService.ToolTip="Add Folders" Click="AddFolder_Click"/>
            <AppBarSeparator />
        </CommandBar>

        <SplitView Grid.Row="1" DisplayMode="Inline" IsPaneOpen="True" PanePlacement="Right" PaneBackground="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <SplitView.Content>
                <ListView x:Name="PendingList">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </SplitView.Content>
            <SplitView.Pane>
                <Grid Background="{StaticResource AcrylicBackgroundFillColorBaseBrush}">
                    <TextBlock Text="PANE" />
                </Grid>
            </SplitView.Pane>
        </SplitView>


    </Grid>
</Page>
